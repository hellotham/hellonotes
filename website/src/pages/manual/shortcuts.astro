---
import ManualLayout from '../../layouts/ManualLayout.astro';

/** Taken from the app's menu definitions, not from memory. */
const groups = [
  {
    title: 'Notes & files',
    rows: [
      ['⌘N', 'New note'],
      ['⌥⌘N', 'New window'],
      ['⇧⌘T', 'Today’s note'],
      ['⌘O', 'Open a collection…'],
      ['⇧⌘O', 'Open Quickly'],
      ['⌃⌘N', 'New note from a prompt…'],
      ['⇧⌘P', 'Command palette'],
      ['⇧⌘R', 'Rename note…'],
      ['⌘D', 'Duplicate note'],
      ['⇧⌘D', 'Add / remove bookmark'],
      ['⇧⌘L', 'Review links…'],
      ['⌘⌫', 'Move note to Trash'],
      ['⌃⌘D', 'Dictate to daily note'],
      ['⌥⌘R', 'Rescan collection'],
      ['⌘P', 'Print…'],
    ],
  },
  {
    title: 'Editing',
    rows: [
      ['⌘F', 'Find & replace in the note'],
      ['⌥⌘F', 'Search all collections'],
      ['⌘B', 'Bold'],
      ['⌘I', 'Italic'],
      ['⌥⌘1 … ⌥⌘3', 'Heading level 1–3'],
      ['⇧⌘7', 'Bulleted list'],
      ['⇧⌘9', 'Numbered list'],
    ],
  },
  {
    title: 'Windows & views',
    rows: [
      ['⌘1 … ⌘4', 'Editor mode: Edit / Preview / Markdown / Split'],
      ['⇧⌘G', 'Graph view'],
      ['⇧⌘J', 'Ask Library'],
      ['⇧⌘A', 'Assistant'],
      ['⌘,', 'Settings'],
    ],
  },
  {
    title: 'System-wide',
    rows: [
      ['⌃⌥⌘N', 'Bring HelloNotes forward and start a new note'],
    ],
  },
];
---

<ManualLayout slug="shortcuts">
  <p>
    Menu commands show their shortcut beside them, so the menu bar is always the authority. This is
    the same list in one place.
  </p>
  <p>
    <strong>These work on iPad too</strong>, with a hardware keyboard — iPadOS shows the same menu
    bar as macOS, and both platforms drive one set of commands rather than two. The exceptions are
    the ones that describe a desktop: <kbd>⌥⌘N</kbd> New Window, Close Tab, Reveal in Finder, Open
    in New Window and Connect Over the Web are absent rather than inert, and the command palette
    (<kbd>⇧⌘P</kbd>) and Graph (<kbd>⇧⌘G</kbd>) appear greyed out because iPad has no surface for
    them yet.
  </p>

  {
    groups.map((g) => (
      <>
        <h2 id={g.title.toLowerCase().replace(/[^a-z]+/g, '-')}>{g.title}</h2>
        <table>
          <thead>
            <tr>
              <th class="w-40">Shortcut</th>
              <th>Action</th>
            </tr>
          </thead>
          <tbody>
            {g.rows.map(([key, action]) => (
              <tr>
                <td>
                  <kbd>{key}</kbd>
                </td>
                <td>{action}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </>
    ))
  }

  <h2 id="elsewhere">Inside particular windows</h2>
  <table>
    <thead>
      <tr>
        <th class="w-40">Shortcut</th>
        <th>Action</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><kbd>⌘W</kbd></td>
        <td>Close the editor tab, when more than one is open — otherwise ⌘W closes the window</td>
      </tr>
      <tr><td><kbd>⌘⏎</kbd></td><td>Append, in the menu-bar quick-capture popover</td></tr>
      <tr><td><kbd>←</kbd> / <kbd>→</kbd></td><td>Previous / next slide, in Slides</td></tr>
    </tbody>
  </table>

  <h2 id="ghost-text">While a suggestion is showing</h2>
  <p>
    These need <strong>Suggest as I type</strong> switched on. The first three act on suggestion
    text that is already visible and do nothing when none is — so <kbd>Esc</kbd> still belongs to
    whatever else is open. <kbd>⌥Esc</kbd> and <kbd>F5</kbd> are the opposite: they ask for a
    suggestion when none is showing.
  </p>
  <table>
    <thead>
      <tr>
        <th class="w-40">Shortcut</th>
        <th>Action</th>
      </tr>
    </thead>
    <tbody>
      <tr><td><kbd>⌥⇥</kbd></td><td>Accept the suggestion</td></tr>
      <tr><td><kbd>→</kbd></td><td>Accept the suggestion</td></tr>
      <tr><td><kbd>Esc</kbd></td><td>Dismiss it</td></tr>
      <tr><td><kbd>⌥Esc</kbd> / <kbd>F5</kbd></td><td>Ask for a suggestion now</td></tr>
    </tbody>
  </table>

  <h2 id="text">Standard macOS shortcuts</h2>
  <p>
    Because the editor is a native text view, every system text shortcut works as it does elsewhere
    on macOS — <kbd>⌘Z</kbd> undo, <kbd>⇧⌘Z</kbd> redo, <kbd>⌥←</kbd>/<kbd>⌥→</kbd> by word,{' '}
    <kbd>⌘↑</kbd>/<kbd>⌘↓</kbd> to the start or end of the document, and the full set of Emacs-style
    control bindings.
  </p>
  <p>
    Writing Tools, dictation, Emoji &amp; Symbols and the system spell checker are all available
    from the Edit menu.
  </p>
</ManualLayout>
