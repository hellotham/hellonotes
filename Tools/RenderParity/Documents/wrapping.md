# Long prose

A long paragraph that must wrap at the sweep width, repeated so that it certainly exceeds one line and exercises the wrap arithmetic on both sides of the comparison. A long paragraph that must wrap at the sweep width, repeated so that it certainly exceeds one line and exercises the wrap arithmetic on both sides of the comparison. A long paragraph that must wrap at the sweep width, repeated so that it certainly exceeds one line and exercises the wrap arithmetic on both sides of the comparison. A long paragraph that must wrap at the sweep width, repeated so that it certainly exceeds one line and exercises the wrap arithmetic on both sides of the comparison. A long paragraph that must wrap at the sweep width, repeated so that it certainly exceeds one line and exercises the wrap arithmetic on both sides of the comparison. A long paragraph that must wrap at the sweep width, repeated so that it certainly exceeds one line and exercises the wrap arithmetic on both sides of the comparison. 

- A list item whose text is also long enough to wrap several times over, because
  a wrapped list item indents its continuation lines and that indent is a place
  the two engines can disagree without any block-level margin being wrong.

> A quoted paragraph long enough to wrap inside the quote, where the gutter bar
  has to be drawn down every one of the wrapped lines rather than only the first.
